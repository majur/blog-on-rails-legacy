<div>
  <h1>Posts</h1>
  <%= render 'dashboard' if logged_in? %>
  <% if Post.any? %>
    <div>
      <%= render 'post' %>
    </div>
    <%# <%= will_paginate @posts_list %>
  <% else %>
    <h3>No posts</h3>
  <% end %>
</div>
