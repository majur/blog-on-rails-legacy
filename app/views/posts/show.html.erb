<p id="notice"><%= notice %></p>
<div>
  <h1>
    <%= @post.title %>
  </h1>

  <div>
    <%= @post.sub_title %>
  </div>

  <p>
    <%= @post.content %>
  </p>
</div>
<% if logged_in? %>
  <%= link_to 'Edit', edit_post_path(@post) %> |
  <%= link_to 'Back', posts_path %>
<% else %>
<% end %>
